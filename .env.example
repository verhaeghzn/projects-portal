APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# Optional: 301 redirect from old domain to APP_URL (path + query preserved, subdomain not retained).
# Exact host: REDIRECT_OLD_DOMAIN=cem-projects-dev.multiscale.nl
# All subdomains: REDIRECT_OLD_DOMAIN=.multiscale.nl

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=projects_portal
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Use "sendmail" on Plesk (uses server's local MTA, no SMTP/SSL needed)
# Use "smtp" or "log" for local development
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
# For sendmail: path is optional; default works on most Plesk/Linux (Postfix)
# MAIL_SENDMAIL_PATH="/usr/sbin/sendmail -bs -i"
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# SURF Conext SAML Configuration
SURF_ENTITY_ID=https://engine.surfconext.nl/authentication/idp/metadata
SURF_SSO_URL=https://engine.surfconext.nl/authentication/idp/single-sign-on
SURF_SLO_URL=https://engine.surfconext.nl/authentication/idp/single-logout
SURF_METADATA_URL=https://metadata.surfconext.nl/idp-metadata.xml
SURF_PUBLIC_CERT_PATH=storage/app/saml/surf_public.crt

# SAML Service Provider (SP) Configuration
SAML_SP_ENTITY_ID=${APP_URL}/saml/metadata
SAML_SP_ACS_URL=${APP_URL}/saml/acs
SAML_SP_SLS_URL=${APP_URL}/saml/sls
SAML_SP_METADATA_URL=${APP_URL}/saml/metadata
SAML_SP_PRIVATE_KEY_PATH=storage/app/saml/sp_private.key
SAML_SP_PUBLIC_CERT_PATH=storage/app/saml/sp_public.crt

# SAML Settings (optional - can also be provided via certificate files)
# SAML_SP_PUBLIC_CERT=
# SAML_SP_PRIVATE_KEY=
# SURF_PUBLIC_CERT=
SAML_STRICT=true
SAML_DEBUG=false
